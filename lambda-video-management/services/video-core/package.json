{
  "name": "video-core-lambda",
  "version": "1.0.0",
  "description": "Core video management service - handles video listing, URLs, deletion, and thumbnails. Deployed via GitHub Actions.",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "deploy": "chmod +x deploy.sh && ./deploy.sh",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.400.0",
    "@aws-sdk/s3-request-presigner": "^3.400.0",
    "@clerk/clerk-sdk-node": "^4.13.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["lambda", "video", "s3", "aws"],
  "author": "Video Management System",
  "license": "MIT"
}